diff --git a/node_modules/git-remote-ops/dist/client.d.ts b/node_modules/git-remote-ops/dist/client.d.ts
index 75ec5ae..0efd4d1 100644
--- a/node_modules/git-remote-ops/dist/client.d.ts
+++ b/node_modules/git-remote-ops/dist/client.d.ts
@@ -11,7 +11,7 @@
 import { Result } from "better-result";
 import type { GitRemoteOpsError } from "./errors.js";
 import { Logger } from "./logger.js";
-import type { CommitInfo, FetchCommitOptions, GitObject, RemoteGitOptions, ServerProfile, TreeEntry } from "./types.js";
+import type { CommitInfo, FetchCommitOptions, FetchTreeOptions, GitObject, RemoteGitOptions, ServerProfile, TreeEntry } from "./types.js";
 /**
  * Read-only Git remote client over smart HTTP.
  *
@@ -76,7 +76,7 @@ export declare class RemoteGit {
         sha: string;
     }, GitRemoteOpsError>>;
     /** Fetch a blob by sha and return its raw contents. */
-    fetchBlob(sha: string): Promise<Result<Uint8Array, GitRemoteOpsError>>;
+    fetchBlob(sha: string, options?: FetchTreeOptions): Promise<Result<Uint8Array, GitRemoteOpsError>>;
     /**
      * Fetch the commit at `ref` *and* its root tree from a single snapshot pack.
      *
@@ -88,8 +88,13 @@ export declare class RemoteGit {
         commitSha: string;
         entries: TreeEntry[];
     }, GitRemoteOpsError>>;
-    /** Fetch and decode a single tree by sha. */
-    fetchTree(sha: string): Promise<Result<TreeEntry[], GitRemoteOpsError>>;
+    private fetchTreeObjects;
+    /** Fetch and decode trees by sha, optionally declaring their shallow commit boundary. */
+    fetchTrees(shas: string[], options?: FetchTreeOptions): Promise<Result<Map<string, TreeEntry[]>, GitRemoteOpsError>>;
+    /** Fetch only tree entry names, without materializing their object IDs. */
+    fetchTreeNames(shas: string[], options?: FetchTreeOptions): Promise<Result<Map<string, string[]>, GitRemoteOpsError>>;
+    /** Fetch and decode a single tree by sha, optionally declaring its shallow commit boundary. */
+    fetchTree(sha: string, options?: FetchTreeOptions): Promise<Result<TreeEntry[], GitRemoteOpsError>>;
     /** Look up an already-materialized object by sha. Does not fetch on miss. */
     getObject(sha: string): Promise<GitObject | undefined>;
     private requiredObject;
diff --git a/node_modules/git-remote-ops/dist/client.js b/node_modules/git-remote-ops/dist/client.js
index b4f0743..006e31f 100644
--- a/node_modules/git-remote-ops/dist/client.js
+++ b/node_modules/git-remote-ops/dist/client.js
@@ -35,6 +35,28 @@ const DEFAULT_CAPS = [
     "agent=git-remote-ops/0.1",
 ];
 const TREE_RETAIN_TYPES = new Set(["commit", "tree"]);
+const TREE_SPACE = 0x20;
+const TREE_NUL = 0x00;
+const TREE_SHA_BYTES = 20;
+const treeDecoder = new TextDecoder();
+function parseTreeNames(content) {
+    const names = [];
+    let offset = 0;
+    while (offset < content.length) {
+        const space = content.indexOf(TREE_SPACE, offset);
+        const nul = space < 0 ? -1 : content.indexOf(TREE_NUL, space + 1);
+        if (space < 0 || nul < 0 || nul + 1 + TREE_SHA_BYTES > content.length) {
+            return fail(new ObjectDecodeError({
+                reason: "truncated-tree-entry",
+                message: "malformed tree entry",
+                objectType: "tree",
+            }));
+        }
+        names.push(treeDecoder.decode(content.subarray(space + 1, nul)));
+        offset = nul + 1 + TREE_SHA_BYTES;
+    }
+    return Result.ok(names);
+}
 function fail(error) {
     return Result.err(error);
 }
@@ -223,11 +245,12 @@ export class RemoteGit {
         return Result.ok({ commit: commit.value, sha: commitSha.value });
     }
     /** Fetch a blob by sha and return its raw contents. */
-    async fetchBlob(sha) {
+    async fetchBlob(sha, options = {}) {
         const profile = this.profile ? Result.ok(this.profile) : await this.discover();
         if (profile.isErr())
             return fail(profile.error);
-        const fetched = await this.fetchObjects([sha]);
+        const shallow = options.shallowCommit ? [options.shallowCommit] : undefined;
+        const fetched = await this.fetchObjects([sha], undefined, undefined, "target", shallow);
         if (fetched.isErr())
             return fail(fetched.error);
         const object = await this.requiredObject(sha);
@@ -273,29 +296,80 @@ export class RemoteGit {
             entries: entries.value,
         });
     }
-    /** Fetch and decode a single tree by sha. */
-    async fetchTree(sha) {
+    async fetchTreeObjects(shas, options = {}) {
         const profile = this.profile ? Result.ok(this.profile) : await this.discover();
         if (profile.isErr())
             return fail(profile.error);
-        const fetched = await this.fetchObjects([sha]);
-        if (fetched.isErr())
-            return fail(fetched.error);
-        const object = await this.requiredObject(sha);
-        if (object.isErr())
-            return fail(object.error);
-        if (object.value.type !== "tree") {
-            return fail(new ObjectDecodeError({
-                reason: "unexpected-object-type",
-                message: `object is not a tree: ${sha}`,
-                objectType: object.value.type,
-                sha,
-            }));
+        const uniqueShas = [...new Set(shas)];
+        if (uniqueShas.length === 0)
+            return Result.ok(new Map());
+        const shallow = options.shallowCommit ? [options.shallowCommit] : undefined;
+        const filter = options.shallowCommit ? "tree:0" : undefined;
+        const pack = await this.fetchPack(uniqueShas, undefined, filter, undefined, shallow);
+        if (pack.isErr())
+            return fail(pack.error);
+        const parseStart = performance.now();
+        const parsed = await this.parsePackFile(pack.value, { targets: new Set(uniqueShas) });
+        if (parsed.isErr())
+            return fail(parsed.error);
+        const counts = parsed.value.stats.byType;
+        this.packLogger.recordPack({
+            bytes: pack.value.length,
+            durationMs: performance.now() - parseStart,
+            byType: counts,
+        });
+        const objects = new Map();
+        for (const sha of uniqueShas) {
+            const object = parsed.value.objects.get(sha);
+            if (!object) {
+                return fail(new ObjectNotFoundError({ sha, message: `object not found: ${sha}` }));
+            }
+            if (object.type !== "tree") {
+                return fail(new ObjectDecodeError({
+                    reason: "unexpected-object-type",
+                    message: `object is not a tree: ${sha}`,
+                    objectType: object.type,
+                    sha,
+                }));
+            }
+            objects.set(sha, object);
+        }
+        return Result.ok(objects);
+    }
+    /** Fetch and decode trees by sha, optionally declaring their shallow commit boundary. */
+    async fetchTrees(shas, options = {}) {
+        const objects = await this.fetchTreeObjects(shas, options);
+        if (objects.isErr())
+            return fail(objects.error);
+        const trees = new Map();
+        for (const [sha, object] of objects.value) {
+            const tree = parseTree(object.content);
+            if (tree.isErr())
+                return fail(tree.error);
+            trees.set(sha, tree.value);
+        }
+        return Result.ok(trees);
+    }
+    /** Fetch only tree entry names, without materializing their object IDs. */
+    async fetchTreeNames(shas, options = {}) {
+        const objects = await this.fetchTreeObjects(shas, options);
+        if (objects.isErr())
+            return fail(objects.error);
+        const trees = new Map();
+        for (const [sha, object] of objects.value) {
+            const names = parseTreeNames(object.content);
+            if (names.isErr())
+                return fail(names.error);
+            trees.set(sha, names.value);
         }
-        const tree = parseTree(object.value.content);
-        if (tree.isErr())
-            return fail(tree.error);
-        return Result.ok(tree.value);
+        return Result.ok(trees);
+    }
+    /** Fetch and decode a single tree by sha. */
+    async fetchTree(sha, options = {}) {
+        const trees = await this.fetchTrees([sha], options);
+        if (trees.isErr())
+            return fail(trees.error);
+        return Result.ok(trees.value.get(sha));
     }
     /** Look up an already-materialized object by sha. Does not fetch on miss. */
     async getObject(sha) {
@@ -308,12 +382,12 @@ export class RemoteGit {
             return fail(object.error);
         return object;
     }
-    async fetchObjects(wants, depth, filterSpec, parseScope = "target") {
+    async fetchObjects(wants, depth, filterSpec, parseScope = "target", shallows) {
         const normalized = await this.normalizeWants(wants, depth, filterSpec);
         if (normalized.length === 0)
             return Result.ok(0);
         this.logger.debug(`fetchObjects wants=${normalized.length} depth=${depth ?? "-"} filter=${filterSpec ?? "-"}`);
-        const pack = await this.fetchPack(normalized, depth, filterSpec);
+        const pack = await this.fetchPack(normalized, depth, filterSpec, undefined, shallows);
         if (pack.isErr())
             return fail(pack.error);
         const parseStart = performance.now();
@@ -402,7 +476,7 @@ export class RemoteGit {
             }
         }
     }
-    async fetchPack(wants, depth, filterSpec, caps) {
+    async fetchPack(wants, depth, filterSpec, caps, shallows) {
         const protocolVersion = this.profile?.protocolVersion ?? 0;
         const requestCaps = caps ?? buildCaps({
             shallow: depth !== undefined,
@@ -414,6 +488,7 @@ export class RemoteGit {
             depth,
             filterSpec,
             protocolVersion,
+            shallows,
         });
         if (body.isErr())
             return fail(body.error);
@@ -458,6 +533,12 @@ export class RemoteGit {
     }
 }
 async function removeIfExists(path) {
-    await rm(path, { force: true });
+    try {
+        await rm(path);
+    }
+    catch (cause) {
+        if (cause?.code !== "ENOENT")
+            throw cause;
+    }
 }
 //# sourceMappingURL=client.js.map
\ No newline at end of file
diff --git a/node_modules/git-remote-ops/dist/index.d.ts b/node_modules/git-remote-ops/dist/index.d.ts
index 4b2fd7c..69f2f96 100644
--- a/node_modules/git-remote-ops/dist/index.d.ts
+++ b/node_modules/git-remote-ops/dist/index.d.ts
@@ -11,5 +11,5 @@ export { LooseObjectStore } from "./store.js";
 export type { LoggerOptions, LogLevel, Metrics } from "./logger.js";
 export { ObjectDecodeError, ObjectNotFoundError, PackParseError, PathNotFoundError, PktLineError, RefNotFoundError, TransportError, UploadPackError, } from "./errors.js";
 export type { GitRemoteOpsError } from "./errors.js";
-export type { CommitInfo, DiagnosticFn, FetchCommitOptions, GitObject, GitObjectMap, GitObjectType, RemoteGitOptions, ServerProfile, TreeEntry, } from "./types.js";
+export type { CommitInfo, DiagnosticFn, FetchCommitOptions, FetchTreeOptions, GitObject, GitObjectMap, GitObjectType, RemoteGitOptions, ServerProfile, TreeEntry, } from "./types.js";
 //# sourceMappingURL=index.d.ts.map
\ No newline at end of file
diff --git a/node_modules/git-remote-ops/dist/pack/parser.js b/node_modules/git-remote-ops/dist/pack/parser.js
index 5e53b97..c353372 100644
--- a/node_modules/git-remote-ops/dist/pack/parser.js
+++ b/node_modules/git-remote-ops/dist/pack/parser.js
@@ -187,6 +187,7 @@ export function parsePackfile(pack, options = {}, sink) {
     };
     let offset = PACK_HEADER_SIZE;
     let pendingRefDeltas = [];
+    let remainingTargets = options.targets?.size;
     function currentResult() {
         return { objects: retainedBySha, stats };
     }
@@ -207,6 +208,9 @@ export function parsePackfile(pack, options = {}, sink) {
             return Result.err(sha.error);
         const object = { type, content };
         stats.materialized++;
+        if (remainingTargets !== undefined && options.targets.has(sha.value) && !baseBySha.has(sha.value)) {
+            remainingTargets--;
+        }
         baseBySha.set(sha.value, object);
         baseByOffset.set(objectOffset, object);
         if (shouldRetain(type)) {
@@ -225,7 +229,7 @@ export function parsePackfile(pack, options = {}, sink) {
         return Result.ok(sha.value);
     }
     function foundTargets() {
-        return options.targets !== undefined && [...options.targets].every((sha) => baseBySha.has(sha));
+        return remainingTargets === 0;
     }
     for (let i = 0; i < count; i++) {
         stats.scanned++;
diff --git a/node_modules/git-remote-ops/dist/protocol/upload_pack.js b/node_modules/git-remote-ops/dist/protocol/upload_pack.js
index 74f3b77..b621b95 100644
--- a/node_modules/git-remote-ops/dist/protocol/upload_pack.js
+++ b/node_modules/git-remote-ops/dist/protocol/upload_pack.js
@@ -75,6 +75,12 @@ function buildV0FetchRequest(options) {
             return Result.err(line.error);
         lines.push(line.value);
     }
+    for (const sha of options.shallows ?? []) {
+        const line = pktLine(encoder.encode(`shallow ${sha}\n`));
+        if (line.isErr())
+            return Result.err(line.error);
+        lines.push(line.value);
+    }
     if (options.depth !== undefined) {
         const line = pktLine(encoder.encode(`deepen ${options.depth}\n`));
         if (line.isErr())
@@ -115,6 +121,12 @@ function buildV2FetchRequest(options) {
             return Result.err(line.error);
         lines.push(line.value);
     }
+    for (const sha of options.shallows ?? []) {
+        const line = pktLine(encoder.encode(`shallow ${sha}\n`));
+        if (line.isErr())
+            return Result.err(line.error);
+        lines.push(line.value);
+    }
     for (const sha of options.wants) {
         const line = pktLine(encoder.encode(`want ${sha}\n`));
         if (line.isErr())
diff --git a/node_modules/git-remote-ops/dist/types.d.ts b/node_modules/git-remote-ops/dist/types.d.ts
index 11c5ae3..00445ee 100644
--- a/node_modules/git-remote-ops/dist/types.d.ts
+++ b/node_modules/git-remote-ops/dist/types.d.ts
@@ -44,6 +44,11 @@ export interface FetchCommitOptions {
     /** Parser retention mode. `trees` keeps commits and trees while skipping blobs. */
     parseScope?: PackParseScope;
 }
+/** Options for fetching a tree that belongs to a shallow commit. */
+export interface FetchTreeOptions {
+    /** Commit SHA declared as shallow while backfilling the tree. */
+    shallowCommit?: string;
+}
 /** Cached server capability profile, populated by `discover()` + `probe()`. */
 export interface ServerProfile {
     url: string;
@@ -107,6 +112,8 @@ export interface FetchRequestOptions {
     depth?: number;
     filterSpec?: string;
     protocolVersion?: 0 | 2;
+    /** Existing shallow boundary OIDs sent before the wants. */
+    shallows?: string[];
 }
 /** What the transport layer returns to the protocol layer. */
 export interface HttpTransportResponse {
