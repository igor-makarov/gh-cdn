{
  "name": "gh-cdn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "patch-package",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run"
  },
  "dependencies": {
    "git-remote-ops": "^0.2.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "patch-package": "^8.0.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0"
  }
}
